# Optimized requirements for Render deployment
# These versions are known to work well on cloud platforms

# Core FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Pydantic - stable versions that don't require compilation
pydantic==2.3.0
pydantic-settings==2.0.3
email-validator==2.0.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Utilities
python-dateutil==2.8.2

# Production server
gunicorn==21.2.0
